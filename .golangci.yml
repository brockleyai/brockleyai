version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    paths:
      - docs
      - documentation
      - deploy
      - web-ui
      - examples

formatters:
  enable:
    - gofmt
